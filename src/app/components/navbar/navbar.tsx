'use client'
import React from 'react'
import { BottomBar, Container, TopBar } from './styled'

const Navbar = () => {
  return (
    <Container>
        <TopBar>

        </TopBar>
        <BottomBar>

        </BottomBar>
    </Container>
  )
}

export default Navbar